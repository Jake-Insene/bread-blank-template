{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build",
            "command": "cmake --build build",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "dependsOn": "CMake Build",
            "label": "CMake Pre Launch",
            "command": "cmake -E copy_directory ${workspaceFolder}/bread/assets/ ${workspaceFolder}/assets/",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}